spring:
  profiles:
    active: test
    include: jwt
  datasource:
    username: SA
    password: 
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    open-in-view: true

file-save-path: <path>
windows-drive: <drive>